# Chat Game Design Document

## 1. 概要

ローカルで動作するWindows向けチャットゲームを開発するための設計書です。

- ユーザー1人が複数のキャラクターと会話しながらストーリーを紡ぐ
- 各キャラクターごとに個別のプロンプト設定を持つ
- 画面は「シーン（Scene）」単位で管理し、一つのチャットが一つのシーンになる
- ローカルLLMを利用し、外部APIに依存しない構成を目指す

## 2. 目的

- Windows環境で安定して動作するローカルアプリを作る
- 会話体験を「ユーザー vs 複数キャラクター」形式で実現する
- キャラクター毎の性格・応答スタイルを分かりやすく設定・確認できるUIを提供する
- シーン単位でストーリーを管理し、物語を構成できる

## 3. 主要要件

### 3.1. 機能要件

- ローカルアプリとして起動できる
- ローカルLLMを呼び出せる
- ユーザーは複数キャラクターから選べる／同時に会話できる
- 各キャラクターのプロンプト（性格・立ち位置）を編集できる
- 1つのチャットを1シーンとして管理できる
- シーンを保存・ロードできる
- 会話履歴はシーン単位で保持される

### 3.2. 非機能要件

- Windows 10/11で動作
- ネットワークを必須としない
- 起動・会話レスポンスは現実的な時間内（数秒以内）
- UIは直感的で、キャラクターの切り替えが分かりやすい

## 4. アーキテクチャ

### 4.1. 推奨構成

- フロントエンド: Electron + React / Vue / Svelte
- バックエンド: Node.js + ローカルLLMランタイム呼び出し
- データストレージ: ローカルファイル（JSON形式）または SQLite
- LLM実行: llama.cpp / GPT4All / Ollama / local model executable

### 4.2. コンポーネント

- UI 層
  - メインウィンドウ
  - シーン管理画面
  - キャラクター設定画面
  - 会話画面

- アプリロジック層
  - シーン管理
  - キャラクター管理
  - シナリオ進行管理
  - LLM 呼び出し管理

- データ層
  - シーンデータ
  - キャラクタープロンプト設定
  - ローカル保存/読み込み

- LLM 層
  - ローカルモデル起動/プロンプト送信
  - レスポンス取得
  - キャラクター別パラメータ反映

## 5. データモデル

### 5.1. Scene

- id
- title
- description
- characters: Character[]
- messages: Message[]
- metadata: { createdAt, updatedAt, tags }

### 5.2. Character

- id
- name
- role
- promptTemplate
- personality
- appearance (表示用アイコン/色)
- active: boolean

### 5.3. Message

- id
- sender: `user` | `character` | `system`
- characterId?: string
- text
- timestamp
- messageType: `user` | `assistant` | `system`

### 5.4. PromptTemplate

- systemPrompt
- behaviorInstructions
- exampleDialog
- temperature
- maxTokens
- topP
- stopSequences

## 6. UI設計

### 6.1. 画面構成

1. ホーム / シーン一覧画面
   - シーンの一覧、作成、読み込み
   - 各シーンの進行状況やタグ表示

2. シーン編集画面
   - シーンタイトルと説明の編集
   - 登場キャラクターリスト
   - キャラクターの追加・並び替え
   - そのシーンで有効なキャラクターのON/OFF

3. 会話画面
   - 左側: シーンの進行情報、キャラクター一覧
   - 中央: 会話ログ
   - 右側: キャラクターのプロンプト/個性設定
   - 下部: ユーザー入力ボックス + 送信ボタン

4. キャラクター設定画面
   - キャラクター個別のプロンプト設定
   - 口調、性格、背景、関係性など
   - モデルパラメータ調整

### 6.2. UIのポイント

- キャラクターごとに色分け/アイコンを付け、視認性を高める
- シーン単位で会話を区切るタブまたはカードを用意
- キャラクターを選択すると、そのキャラクターに特化したプロンプトが右側に表示される
- 「シーンを切り替える」「新しいシーンを追加する」を常にアクセス可能にする

## 7. ローカルLLM統合

### 7.1. 方式

- ローカルLLMランタイムを直接呼び出す
  - 例: `llama.cpp`のバイナリにプロンプトを渡す
  - 例: `GPT4All`のローカルエンドポイントを利用
- または、ローカルAPIを用意してフロントエンドからHTTPで通信する

### 7.2. 推奨設定

- 各キャラクタープロンプトから生成プロンプトを構築
- 生成時にキャラクターの役割を反映した system prompt を前置
- ユーザーの直前発言 + 履歴から文脈をつなぐ
- キャラクターの個性に応じて temperature / top_p を調整可能

### 7.3. LLM呼び出し例フロー

1. ユーザーが入力
2. 該当シーンの会話履歴を収集
3. 対象キャラクターの `PromptTemplate` を取得
4. ローカルLLMへのリクエストを生成
5. LLMからの返信を受け取り、`Message`として保存
6. UIに表示

## 8. フロー設計

### 8.1. シーン開始

- シーン作成時に登場キャラクターを選択
- キャラクターの初期 system prompt をセット
- 会話ログはシーン単位に開く

### 8.2. 会話進行

- ユーザー入力→送信
- シーン内全キャラクターに同時反応させるか、1キャラクターだけ返信させるか設定
- 1つのシーンを複数キャラクターの会話空間として扱う
- キャラクター間の発話を追跡し、ストーリー感を出す

### 8.3. キャラクターの役割分担

- `main` キャラクター: ユーザーとの直接応答担当
- `support` キャラクター: 主に状況説明や関係性のサポート
- `narrator` / `observer`: ワールド設定や場面転換を補助

## 9. 発展機能案

- キャラクター別の「返答優先度」や「発話頻度」設定
- キャラクター間の関係性ツリー表示
- シーン一覧からストーリー全体を可視化するタイムライン
- シーン間で変化する状態変数の継承
- セーブ/ロード機能、エクスポート/インポート
- ローカルモデルのダウンロードと管理機能

## 10. 技術選定の比較

### 10.1. Electron + React + Node.js

- 長所
  - UI実装が速い
  - Windowsデスクトップアプリとして配布しやすい
  - NodeからローカルLLMバイナリやPythonプロセスを呼べる
- 短所
  - アプリサイズが大きくなりやすい

### 10.2. Python + PySide/PyQt

- 長所
  - ローカルLLMとの統合が進めやすい
  - 軽量で学習コストが低い
- 短所
  - Windows UIのネイティブ感は弱い場合がある

### 10.3. .NET / WinUI

- 長所
  - Windowsに最適化されたネイティブUI
  - デスクトップアプリとして安定
- 短所
  - ローカルLLM連携実装で追加ラッパーが必要になる可能性

## 11. MVP提案

- 画面: シーン一覧、1つの会話画面、キャラクター設定画面
- 機能: シーン作成、会話送信、複数キャラクターを1シーンで管理、ローカルLLM呼び出し
- 画面表示: キャラクター別チャットバブルとプロンプト設定パネル
- 保存: JSONベースのローカルファイル保存

## 12. 次の作業

1. プロジェクト構成を決定する
2. UI画面のワイヤーフレームを作成する
3. ローカルLLM実行環境を決める（llama.cpp / GPT4All など）
4. 最初のMVPを実装する

---

作成日: 2026-05-09
